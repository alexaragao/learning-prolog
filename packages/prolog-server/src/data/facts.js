const facts = [
  // Fruit Ninja fruits
  "fruit(watermelon). fruit(mango). fruit(pineapple). fruit(coconut). fruit(strawberry). fruit(green_apple). fruit(red_apple). fruit(banana). fruit(lemon). fruit(lime). fruit(orange). fruit(plum). fruit(pear). fruit(passion_fruit).",
  // Colors
  "red(red_apple). red(strawberry). yellow(banana). yellow(lemon). orange(orange). orange(mango). brown(coconut). brown(kiwi).",
  // Eats
  "eats(john, red_apple). eats(john, banana). eats(john, kiwi). eats(maria, green_apple). eats(maria, banana). eats(maria, watermelon).",
  // Apples
  "apple(red_apple). apple(green_apple)."
];

module.exports.facts = facts;